<form class="{{cssClass}}" autocomplete="off">

	<header>
		<div class="image">
			<img src="{{item.img}}" data-edit="img">
		</div>
		<div class="fields">
			<fieldset>
				<label>{{localize "Name"}}</label>
				<input name="name" value="{{item.name}}">
			</fieldset>
			<fieldset>
				<label>Type</label>{{!-- TODO: Localize --}}
				<span>{{system.type.label}}</span>
			</fieldset>
		</div>
		<nav data-group="primary">
			<a data-tab="description">Description</a>{{!-- TODO: Localize --}}
			<a data-tab="details">Details</a>{{!-- TODO: Localize --}}
		</nav>
	</header>

	<main>
		<div class="tab" data-group="primary" data-tab="description">
			{{editor enriched.description target="system.description.value" button=true editable=editable
				engine="prosemirror" collaborate=false}}
		</div>
		<div class="tab" data-group="primary" data-tab="details">
			<section>
				<fieldset>
					<label>Activation Cost</label>{{!-- TODO: Localize --}}
					<select name="system.activation.type">
						{{selectOptions CONFIG.actionTypesStandard selected=system.activation.type blank=(localize "None")}}
					</select>
				</fieldset>
				<fieldset>
					<label>Activation Condition</label>{{!-- TODO: Localize --}}
					<input type="text" name="system.activation.condition" value="{{system.activation.condition}}">
				</fieldset>
				<fieldset>
					<label>Consumption</label>{{!-- TODO: Localize --}}
					<div class="fields">
						<label>{{!-- TODO: Should span full width if no type selected --}}
							<span>Type</span>{{!-- TODO: Localize --}}
							<select name="system.resource.type">
								{{selectOptions CONFIG.consumptionTypes selected=system.resource.type labelAttr="label" blank=""}}
							</select>
						</label>
						{{#if system.resource.type}}
							<label>
								<span>Amount</span>{{!-- TODO: Localize --}}
								<input type="number" name="system.resource.amount" value="{{system.resource.amount}}">
							</label>
							<label>
								<span>Target</span>{{!-- TODO: Localize --}}
								{{#if item.isEmbedded}}
									Select Here
								{{else}}
									<input type="text" name="system.resource.target" value="{{source.resource.target}}">
								{{/if}}
							</label>
						{{/if}}
					</div>
				</fieldset>
				<fieldset class="triple">
					<label>Limited Uses</label>{{!-- TODO: Localize --}}
					<div class="fields">
						<label>
							<span>Max Uses</span>{{!-- TODO: Localize --}}
							<input type="text" name="system.uses.max" value="{{source.uses.max}}">
						</label>
						<label>
							<span>Recovery Period</span>{{!-- TODO: Localize --}}
							<select name="system.uses.per">
								{{selectOptions CONFIG.recoveryPeriods selected=system.uses.per labelAttr="label" blank=""}}
							</select>
						</label>
						<label>
							<span>Amount Recovered</span>{{!-- TODO: Localize --}}
							<input type="text" name="system.uses.recovery" value="{{system.uses.recovery}}" placeholder="All">
						</label>
				</fieldset>
			</section>
		</div>
	</main>

</form>
